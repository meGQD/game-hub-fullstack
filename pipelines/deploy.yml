.deploy_rules:
  stage: deploy
  environment: 
    name: prod 
  tags:
    - prod

deploy prod:
  when: manual
  script:
    - helm install backend ./kubernetes/charts/backend
    - helm install frontend ./kubernetes/charts/frontend


# deploy beta:
#   stage: deploy
#   environment: 
#     name: beta
#   tags:
#     - beta
#   when: always
#   script:
#     - docker compose down || docker compose -f docker-compose.cicd.yml down
#     - docker compose up --build -d
